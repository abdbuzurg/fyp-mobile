import 'dart:ui';

import 'package:flutter/material.dart';

const appPrimaryColor = Color.fromRGBO(255, 163, 26, 1.0);
const appSecondaryColor = Color.fromRGBO(0, 8, 179, 1);
const appIconColor = Color.fromRGBO(255, 163, 26, 0.75);

const backendApiUrl = 'https://fyp-new-backend.herokuapp.com/api/v1/';

const TextStyle titleTextStyle =
    TextStyle(fontSize: 20.0, fontWeight: FontWeight.bold, letterSpacing: 2);
